# Minimal deps for this fork's active path (voicechat2.py, srt-server.py,
# tts-server-piper.py): no torch/transformers/coqui-tts/whisperx, since SRT
# bridges to an existing Whisper container and TTS shells out to the Piper
# binary instead of loading models in-process. See CLAUDE.md.
#
# Recreate with: python3.12 -m venv .venv && source .venv/bin/activate \
#   && pip install -r requirements-lean.txt
#
# requirements.txt (upstream, unchanged) is still needed only if you run the
# original tts-server.py (coqui) or srt-server.py's TransformersEngine/
# FasterWhisperEngine instead of this fork's defaults.

aiohttp==3.14.3
fastapi==0.141.1
loguru==0.7.3
mutagen==1.48.1
numpy==2.5.2
python-multipart==0.0.32
requests==2.34.2
uvicorn[standard]==0.52.4
websockets==17.1
